const lado1 = 3;
const lado2 = 13;

const area = lado1 * lado2;
const perimetro = 2 * (lado1 + lado2);

console.log(`Área del rectangulo: ${area} cm²`);
console.log(`Perimetro del rectangulo: ${perimetro} cm`);